import type { backendInterface } from "../backend";

export const mockBackend: backendInterface = {
  _caffeineStorageBlobIsLive: async () => true,
  _caffeineStorageBlobsToDelete: async () => [],
  _caffeineStorageConfirmBlobDeletion: async () => undefined,
  _caffeineStorageCreateCertificate: async () => ({ method: "", blob_hash: "" }),
  _caffeineStorageRefillCashier: async () => ({ success: undefined, topped_up_amount: undefined } as any),
  _caffeineStorageUpdateGatewayPrincipals: async () => undefined,
  _initializeAccessControlWithSecret: async () => undefined,
  addGalleryItem: async () => BigInt(0),
  assignCallerUserRole: async () => undefined,
  deleteGalleryItem: async () => undefined,
  deleteReview: async () => undefined,
  getAllGalleryItems: async () => [],
  getAllReviews: async () => [
    {
      id: BigInt(1),
      name: "Priya Sharma",
      rating: BigInt(5),
      review: "Absolutely the best dal makhani in Burari! The flavours are so authentic and rich. Feels like home cooking.",
      createdAt: BigInt(Date.now() * 1_000_000 - 86400_000_000_000),
    },
    {
      id: BigInt(2),
      name: "Rahul Verma",
      rating: BigInt(5),
      review: "Chole bhature are incredible! Crispy and perfectly spiced. My whole family loves ordering from here.",
      createdAt: BigInt(Date.now() * 1_000_000 - 172800_000_000_000),
    },
    {
      id: BigInt(3),
      name: "Anita Kapoor",
      rating: BigInt(5),
      review: "10+ years and the taste has never changed. That consistency is what makes Raje Di Hatti special.",
      createdAt: BigInt(Date.now() * 1_000_000 - 259200_000_000_000),
    },
    {
      id: BigInt(4),
      name: "Deepak Joshi",
      rating: BigInt(5),
      review: "The Amritsari Kulcha is next level. Served piping hot with perfectly made chole. Highly recommended!",
      createdAt: BigInt(Date.now() * 1_000_000 - 345600_000_000_000),
    },
  ],
  getAllSubmissions: async () => [],
  getCallerUserProfile: async () => null,
  getCallerUserRole: async () => "user" as any,
  getGalleryItem: async () => null,
  getLatestReviews: async (limit) => [
    {
      id: BigInt(10),
      name: "Simran Kaur",
      rating: BigInt(5),
      review: "The paneer dishes are to die for. Ordered via Zomato and got it in 20 mins. Fresh and hot!",
      createdAt: BigInt(Date.now() * 1_000_000 - 3600_000_000_000),
    },
    {
      id: BigInt(11),
      name: "Manish Gupta",
      rating: BigInt(4),
      review: "Great taste, good portions. The Special Thali is a steal at this price. Will order again!",
      createdAt: BigInt(Date.now() * 1_000_000 - 7200_000_000_000),
    },
  ],
  getOrderById: async () => null,
  getReviewInputs: async () => [],
  getReviewsPaginated: async () => [
    {
      id: BigInt(20),
      name: "Kavita Singh",
      rating: BigInt(5),
      review: "Been eating here since childhood. The taste is still the same — pure gold. Burari's pride!",
      createdAt: BigInt(Date.now() * 1_000_000 - 1800_000_000_000),
    },
  ],
  getUserProfile: async () => null,
  isCallerAdmin: async () => false,
  saveCallerUserProfile: async () => undefined,
  submitContactForm: async () => undefined,
  submitOrder: async () => "ORD-1234",
  submitReview: async () => BigInt(99),
};
